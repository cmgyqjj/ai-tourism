/* 行程PK页面样式 */
.strategy-pk-container {
    width: 100%;
    height: 100vh;
    background-color: #f5f5f5;
    display: flex;
    flex-direction: column;
  }
  
  
  /* 导航栏 */
  .nav-bar {
    display: flex;
    align-items: center;
    justify-content: center;
    padding: 62rpx 32rpx 24rpx;
    background-color: #fff;
    position: relative;
    z-index: 100;
    margin-top: 38rpx;
  }
  
  .nav-left {
    position: absolute;
    left: 32rpx;
    width: 48rpx;
    height: 48rpx;
    display: flex;
    align-items: center;
    justify-content: center;
  }
  
  .back-icon {
    display: inline-block;
    font-size: 70rpx;
    line-height: 32rpx;
    color: #000;
  }
  
  .nav-title {
    font-size: 36rpx;
    font-weight: 600;
    color: #333;
    text-align: center;
  }
  
  /* 标签页导航 */
  .tab-nav {
    display: flex;
    background-color: #fff;
    padding: 0 32rpx;
    border-bottom: 1rpx solid #f0f0f0;
  }
  
  .tab-item {
    flex: 1;
    text-align: center;
    padding: 32rpx 0;
    position: relative;
    color: #666;
    font-size: 32rpx;
  }
  
  .tab-item.active {
    color: #333;
    font-weight: 600;
  }
  
  .tab-underline {
    position: absolute;
    bottom: 0;
    left: 50%;
    transform: translateX(-50%);
    width: 60rpx;
    height: 6rpx;
    background-color: #FFD700;
    border-radius: 3rpx;
  }
  
  /* 内容区域 */
  .content-area {
    flex: 1;
    padding: 0 0 160rpx 0;
  }
  
  .basic-info-content {
    padding: 10rpx;
  }
  
  /* 通用section样式 */
  .section {
    margin-bottom: 48rpx;
    background-color: #fff;
    border-radius: 16rpx;
    overflow: hidden;
  }
  
  
  /* 预算价格section收起时的间距调整 */
  .budget-section {
    margin-bottom: 24rpx;
  }
  
  .header-container {
    display: flex;
    align-items: center; /* 垂直居中对齐 */
    justify-content: flex-start; /* 左对齐 */
    padding: 16rpx 0; /* 减少上下内边距 */
    min-height: 60rpx; /* 减少最小高度 */
  }
  
  .section-title {
    width:100px;
    
    font-size: 32rpx;
    margin-bottom: 8rpx; /* 减少底部间距 */
  }

  
  .section-header {
    display: flex;
    align-items: center;
    justify-content: space-between;
      cursor: pointer;
    min-height: 88rpx;
  }
  
  .arrow-icon {
    font-size: 18rpx;
    color: rgb(22, 21, 21);
    transition: all 0.3s ease;
    transform-origin: center;
    margin-right: 570rpx; /* 收起状态：靠右显示 */
    line-height: 1;
  }
  
  .arrow-icon.expanded {
    transform: rotate(180deg);
    margin-right: 0; /* 展开状态：移除右边距 */
    margin-left: 16rpx; /* 展开状态：靠左显示 */
  }
  
  /* .arrow-icon.expanded {
    transform: rotate(180deg);
  }
   */
  /* 已有方案 */
  .existing-plans {
    background-color: transparent;
    margin-bottom: 24rpx;
  }
  
  
  .plans-container {
    padding: 0 16rpx;
    height: 400rpx;
    overflow-x: auto;
  }
  
  .plans-wrapper {
    display: flex;
    gap: 20rpx;
    padding-right: 32rpx;
    flex-wrap: nowrap;
  }
  
  .plan-card {
    flex: 0 0 auto;
    width: 320rpx;
    background-color: #fff;
    border-radius: 16rpx;
    overflow: hidden;
    box-shadow: 0 4rpx 20rpx rgba(0,0,0,0.08);
    height: 360rpx;
    margin-right: 24rpx;
  }
  
  .plan-status {
    position: absolute;
    top: 16rpx;
    left: 16rpx;
    z-index: 10;
  }
  
  
  .status-badge {
    display: flex;
    align-items: center;
    justify-content: center;
    padding: 8rpx 16rpx;
    border-radius: 20rpx;
    font-size: 24rpx;
    min-width: 90rpx;
  }
  
  .status-badge.not-voted {
    background-color: #333;
    color: #fff;
  }
  
  .status-badge.voted {
    background-color: #FFD700;
    color: #333;
  }
  
  .check-icon {
    display: inline-block;
    font-size: 20rpx;
    margin-right: 8rpx;
    width: 24rpx;
    height: 24rpx;
    background-color: rgba(255, 255, 255, 0.2);
    border-radius: 50%;
    display: flex;
    align-items: center;
    justify-content: center;
    line-height: 1;
  }
  
  .status-badge.voted .check-icon {
    background-color: rgba(25, 4, 4, 0.27);
  }
  
  .plan-content {
    position: relative;
    padding: 24rpx;
    text-align: center;
    display: flex;
    flex-direction: column;
    height: 320rpx;
    justify-content: space-between;
  }
  
  .title-row {
    width: 100px;
    display: flex;
    align-items: left;
    justify-content: center;
    gap: 8rpx;
    margin-bottom: 12rpx;
  }
  
  .plan-icon {
    width: 32rpx;
    height: 32rpx;
    display: flex;
    align-items: center;
    justify-content: center;
    flex-shrink: 0;
  }
  
  .plan-icon .icon {
    width: 100%;
    height: 100%;
  }
  
  /* text icon inside plan card */
  .plan-icon .icon-text {
    font-size: 28rpx;
    line-height: 1;
  }
  
  .plan-title {
    font-size: 28rpx;
    font-weight: 600;
    color: #333;
  }
  
    .plan-subtitle {
    font-size: 22rpx;
    color: #999;
    text-align: left;
    margin-left: 0;
  }
  
        .main-content { 
      flex: 1;
      display: flex;
      flex-direction: column;
      justify-content: center;
      align-items: center;
      margin-top: 150rpx;
    }
  
  .vote-section {
    display: flex;
    align-items: center;
    justify-content: space-between;
 
  }
  
  .avatar {
    width: 48rpx;
    height: 48rpx;
    border-radius: 50%;
  }
  
  .vote-btn {
    display: flex;
    align-items: center;
    padding: 6rpx 12rpx;
    font-size: 22rpx;
    color: #007AFF;
    background: none;
    border: none;
    text-decoration: underline;
    text-decoration-color: #007AFF;
    text-underline-offset: 6rpx;
    text-decoration-thickness: 2rpx;
  }
  
  .vote-btn.voted {
    color: #999;
    background: none;
    border: none;
    text-decoration: none;
  }
  
  .vote-btn:active {
    opacity: 0.7;
  }
  
  .avatar-badge {
    display: flex;
    align-items: center;
    justify-content: center;
    width: 40rpx;
    height: 40rpx;
    border-radius: 50%;
    overflow: hidden;
  }
  
  .avatar-img {
    width: 100%;
    height: 100%;
    border-radius: 50%;
  }
  
  .vote-icon {
    display: inline-block;
    font-size: 20rpx;
    margin-right: 6rpx;
    color: #D2B48C;
  }
  
  /* share icon image */
  .share-icon {
    display: inline-block;
    width: 26rpx;
    height: 26rpx;
    margin-right: 8rpx;
    vertical-align: middle;
  }
  
  /* 方案亮点 */
  .plan-highlights {
    background-color: transparent;
    margin-bottom: 24rpx;
  }
  
  .highlights-container {
    padding: 0 16rpx;
    height: 450rpx;
    overflow-x: auto;
  }
  
  .highlights-wrapper {
    display: flex;
    gap: 24rpx;
    padding-right: 32rpx;
    height: 100%;
    flex-wrap: nowrap;
  }
  
  .highlight-card {
    flex-shrink: 0;
    background-color: #fff;
    border-radius: 16rpx;
    padding: 32rpx 32rpx;
    display: flex;
    flex-direction: column;
    min-height: 200rpx; /* 确保卡片有足够高度 */
  }
  
  .highlight-content {
    font-size: 26rpx;
    color: #333;
    line-height: 1.5;
    margin-bottom: 24rpx;
    text-align: left;
    word-wrap: break-word;
    white-space: normal; /* 允许换行 */
  }
  
  .highlight-tags {
    display: flex;
    flex-wrap: wrap; /* 允许标签换行 */
    gap: 8rpx;
    justify-content: flex-start;
    width: 100%; /* 确保宽度足够容纳标签 */
    min-height: 80rpx; /* 给标签换行预留空间 */
    align-content: flex-start; /* 从顶部开始排列 */
  }
  
  .tag {
    background-color: #FFD700;
    color: #333;
    font-size: 22rpx;
    padding: 8rpx 16rpx;
    border-radius: 20rpx;
    white-space: normal;
    margin-bottom: 8rpx;
  }
  

  /* 可展开section内容 */
   .budget-content,
  .exclusive-content,
  .accommodation-content,
  .transportation-content {
    max-height: 0;
    overflow: hidden;
    transition: max-height 0.3s ease;
  }
  
  .budget-content.expanded,
  .exclusive-content.expanded,
  .accommodation-content.expanded,
  .transportation-content.expanded {
    max-height: none;
    padding-bottom: 32rpx;
  }
  
  .budget-content.collapsed,
  .exclusive-content.collapsed,
  .accommodation-content.collapsed,
  .transportation-content.collapsed {
    max-height: 0;
    padding-bottom: 0;
  }
  
  /* 预算价格收起时减少底部间距 
  .budget-content.collapsed {
    margin-bottom: -24rpx;
  }
  
  /* 横向滑动容器 */
  .budget-container {
    text-align: center;
    white-space: nowrap;  
    overflow-x: auto;
    padding: 0 32rpx;
    height: 310rpx; 
  }
  .exclusive-container {
    text-align: center;
    white-space: nowrap;  
    overflow-x: auto;
    padding: 0 32rpx;
    height: 410rpx; 
  }
  .accommodation-container,
  .transportation-container {
    text-align: center;
    white-space: nowrap;  
    overflow-x: auto;
    padding: 0 32rpx;
    height: 210rpx; 
  }

  
  .budget-wrapper,
  .exclusive-wrapper,
  .accommodation-wrapper,
  .transportation-wrapper {
    display: flex;
    min-width: 100%;
    gap: 0;
    padding-right: 32rpx;
    width: 100%;
    flex-direction: column;
  } 
  
  /* 预算价格行 */
/* 预算价格section收起时的间距调整 */
.budget-section {
    margin-bottom: 24rpx;
  }
  
  .header-container {
    display: flex;
    align-items: center; /* 垂直居中对齐 */
    justify-content: flex-start; /* 左对齐 */
  }
  
  .section-title {
    text-align: center;
    width:100px;
    font-size: 32rpx;
  }
  
  .section-header {
    display: flex;
    align-items: center;
    justify-content: space-between;
    /* padding:0rpx 0rpx 24rpx; */
    cursor: pointer;
    min-height: 88rpx;
  }
  
  .arrow-icon {
    font-size: 18rpx;
    color: rgb(22, 21, 21);
    transition: all 0.3s ease;
    transform-origin: center;
    margin-right: 570rpx; /* 收起状态：靠右显示 */
    line-height: 1;
  }
  
  .arrow-icon.expanded {
    transform: rotate(180deg);
    margin-right: 0; /* 展开状态：移除右边距 */
    margin-left: 16rpx; /* 展开状态：靠左显示 */
  }
  
  /* 预算价格内容展开和收起 */
/* 通用内容样式 */
/* 通用内容展开/收起样式 */
/* 展开内容 */
.content {
    max-height: none; /* 动态计算，不限制最大高度 */
    overflow: hidden;
    transition: max-height 0.3s ease;
  }
  
  /* 展开时显示内容 */
  .content.expanded {
    padding-bottom: 32rpx;
  }
  
  /* 收起时隐藏内容 */
  .content.collapsed {
    max-height: 0;
    padding-bottom: 0;
  }
  
  /* 内容容器 */
  .container {
    white-space: nowrap;
    overflow-x: auto;
    padding: 0 32rpx;
    height: auto; /* 根据内容自动调整高度 */
  }
  
  /* 包装器 */
  .wrapper {
    display: flex;
    flex-wrap: wrap; /* 自动换行 */
    gap: 16rpx;
    width: 100%;
  }
  
  /* 行 */
  /* 行容器，确保内容能自适应 */
.row {
    display: flex;
    align-items: center;
    flex-wrap: wrap; /* 允许换行 */
    padding: 16rpx 0;
    border-bottom: 1rpx solid #f0f0f0;
    width: 100%;
    word-break: break-word;  /* 强制文字换行 */
  }
  
  .row:last-child {
    border-bottom: none;
  }
  
  /* 标签 */
  .label {
    width: 120rpx;
    font-size: 28rpx;
    color: #666;
    flex-shrink: 0;
    margin-right: 16rpx;  /* 给标签增加一些右边距 */
  }
  
  /* 内容值 */
  .values {
    display: flex;
    flex-wrap: wrap; /* 允许值内容换行 */
    gap: 12rpx;  /* 调整项之间的间距 */
    flex: 1;
    padding: 8rpx;
    border-radius: 8rpx;
    overflow-x: auto;
    white-space: normal;  /* 允许内容换行 */
    justify-content: flex-start;
  }
  
  /* 每个值项 */
  .value {
    font-size: 26rpx;
    color: #333;
    text-align: center;
    width: auto;  /* 自适应宽度 */
    padding: 8rpx;
    white-space: nowrap;
    word-break: break-word;  /* 允许文字断行 */
  }
  
  
  
  /* 针对每个部分（budget, exclusive, accommodation, transportation）定义的特定类 */
  
  /* 预算 */
  .budget-content,
  .exclusive-content,
  .accommodation-content,
  .transportation-content {
    /* max-height: 0; */
    overflow: hidden;
    transition: max-height 0.3s ease;
  }
  
  .budget-content.expanded,
  .exclusive-content.expanded,
  .accommodation-content.expanded,
  .transportation-content.expanded {
    max-height: 1000rpx;
    /* padding-bottom: 32rpx; */
  }
  
  .budget-content.collapsed,
  .exclusive-content.collapsed,
  .accommodation-content.collapsed,
  .transportation-content.collapsed {
    /* max-height: 0; */
    padding-bottom: 0;
  }
  
  /* 每个部分的容器样式 */
  .budget-container,
  .exclusive-container,
  .accommodation-container,
  .transportation-container {
    white-space: nowrap;
    overflow-x: auto;
    padding: 0 0rpx;
    max-height: 400rpx; /* 统一使用400rpx高度,.............. */
  }
  

  
  /* 每个部分的包装样式 */
  .budget-wrapper,
  .exclusive-wrapper,
  .accommodation-wrapper,
  .transportation-wrapper {
    display: flex;
    gap: 24rpx;
    width: 100%;
  }
  
  /* 每个部分的行样式 */
  .budget-row,
  .exclusive-row,
  .accommodation-row,
  .transportation-row {
    display: flex;
    align-items: flex-start;
    padding: 16rpx 0;
    border-bottom: 1rpx solid #f0f0f0;
    width: 100%;
    min-height: 60rpx;
  }
  
  .budget-row:last-child,
  .exclusive-row:last-child,
  .accommodation-row:last-child,
  .transportation-row:last-child {
    border-bottom: none;
  }
  
  /* 每个部分的标签样式 */
  .budget-label,
  .exclusive-label,
  .accommodation-label,
  .transportation-label {
    width: 120rpx;
    font-size: 24rpx;
    color: #000;
    flex-shrink: 0;
    margin-right: 16rpx;
    margin-left: 32rpx;
    padding-top: 18rpx;
  }
  
  /* 每个部分的值容器样式 */
  .budget-values,
  .exclusive-values,
  .accommodation-values,
  .transportation-values {
    display: flex;
    flex-wrap: wrap; /* 允许值内容换行 */
    gap: 16rpx;  /* 调整项之间的间距 */
    flex: 1;
    padding: 8rpx;
    border-radius: 8rpx;
    align-items: flex-start;
    justify-content: flex-start;
  }
  
  /* 每个部分的单项值样式 */
  .budget-value,
  .exclusive-value,
  .accommodation-value,
  .transportation-value {
    font-size: 26rpx;
    color: #333;
    text-align: center;
    min-width: 80rpx;
    max-width: 150rpx;
    width: auto;
    padding: 8rpx 12rpx;
    background-color: #f8f8f8;
    border-radius: 8rpx;
    white-space: normal;
    word-wrap: break-word;
    line-height: 1.4;
    flex-shrink: 0;
  }


  /* 底部按钮 */
  .bottom-actions {
    position: fixed;
    bottom: 0;
    left: 0;
    right: 0;
    display: flex;
    padding: 24rpx 32rpx;
    background-color: #fff;
    border-top: 1rpx solid #f0f0f0;
    gap: 24rpx;
  }
  
  .action-btn {
    flex: 1;
    height: 88rpx;
    border-radius: 44rpx;
    display: flex;
    align-items: center;
    justify-content: center;
    font-size: 28rpx;
    font-weight: 600;
  }
  
  .action-btn.secondary {
    background-color:#9a919194;
    color: #666;
    border: 1rpx solid #e0e0e0;
  }
  
  .action-btn.primary {
    background-color: #333;
    color: #fff;
  }
  
  .share-icon {
    width: 24rpx;
    height: 24rpx;
    margin-right: 8rpx;
  }
  
  
  
  /* 响应式设计：对于小屏设备调整样式 */
  @media (max-width: 375px) {
    /* 调整整体字体大小 */
    .section-title {
      font-size: 28rpx;
    }
  

  }
  

  
  
  
  

  
  /* AI融合页面 */
  .ai-fusion-content {
    padding: 8rpx 16rpx 120rpx;
    min-height: calc(100vh - 200rpx);
  }
  
  
  .fusion-plan-card {
    background-color: #fff;
    border-radius: 16rpx;
    overflow: hidden;
    box-shadow: 0 4rpx 20rpx rgba(0,0,0,0.08);
    height: 400rpx;
    display: flex;
    flex-direction: column;
    width: 100%;
    position: relative;
  }
  
  /* AI融合页面卡片内容样式 - 复用通用样式 */
  .fusion-plan-card .plan-status {
    position: absolute;
    top: 8rpx;
    left: 8rpx;
    padding: 0;
    background-color: transparent;
    transform: none;
  }
  
  /* 缩小投票徽标尺寸，做成左上角小胶囊 */
  .fusion-plan-card .plan-status .status-badge {
    padding: 4rpx 12rpx;
    font-size: 20rpx; /* 再缩小一半视觉体量 */
    border-radius: 24rpx;
  }
  .fusion-plan-card .plan-status .status-badge .check-icon {
    font-size: 18rpx;
    margin-right: 6rpx;
  }
  
  .fusion-plan-card .plan-content {
    flex: 1;
    padding: 150rpx 20rpx 20rpx; /* 增加顶部间距，将文字内容下移 */
    text-align: center;
    display: flex;
    flex-direction: column;
    justify-content: space-between;
  }
  
  .fusion-plan-card .title-row {
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 16rpx;
    margin-bottom: 8rpx; /* 调整标题与副标题的间距 */
  }
  
  .fusion-plan-card .plan-icon {
    width: 36rpx;
    height: 36rpx;
    display: flex;
    align-items: center;
    justify-content: center;
    flex-shrink: 0;
  }
  
  .fusion-plan-card .icon-text {
    font-size: 28rpx;
    line-height: 1;
    margin-top: 0;
  }
  
  .fusion-plan-card .plan-title {
    font-size: 30rpx;
    font-weight: 600;
    color: #333;
    line-height: 1.2;
  }
  
  .fusion-plan-card .plan-subtitle {
    font-size: 26rpx;
    color: #666;
    margin-top: 4rpx;
    margin-bottom: 20rpx; /* 调整与投票区域的间距 */
    line-height: 1.2;
  }
  
  .fusion-plan-card .vote-section {
    display: flex;
    align-items: center;
    justify-content: space-between;
    margin-top: 20rpx;
  }
  
  .fusion-plan-card .avatar-badge {
    display: flex;
    align-items: center;
    justify-content: center;
    width: 40rpx;
    height: 40rpx;
    border-radius: 50%;
    overflow: hidden;
  }
  
  .fusion-plan-card .avatar-img {
    width: 100%;
    height: 100%;
    border-radius: 50%;
  }
  
  .fusion-plan-card .vote-btn {
    display: flex;
    align-items: center;
    padding: 6rpx 12rpx;
    font-size: 22rpx;
    color: #007AFF;
    background: none;
    border: none;
    text-decoration: underline;
    text-decoration-color: #007AFF;
    text-underline-offset: 6rpx;
    text-decoration-thickness: 2rpx;
  }
  
  .fusion-plan-card .vote-btn.voted {
    color: #999;
    background: none;
    border: none;
    text-decoration: none;
  }
  
  .fusion-plan-card .vote-icon {
    display: inline-block;
    font-size: 20rpx;
    margin-right: 6rpx;
    color: #D2B48C;
  }
  
  
  
  /* AI融合按钮 */
  .ai-fusion-actions {
    position: fixed;
    bottom: 0;
    left: 0;
    right: 0;
    padding: 24rpx 32rpx 24rpx 32rpx;
    background-color: #fff;
    border-top: 1rpx solid #f0f0f0;
    padding-top: 40rpx;
  }
  
  .ai-fusion-btn {
    width: 100%;
    height: 88rpx;
    border-radius: 44rpx;
    display: flex;
    align-items: center;
    justify-content: center;
    font-size: 32rpx;
    font-weight: 600;
    color: #333;
    gap: 16rpx;
    position: relative;
    overflow: hidden;
  }
  
  .ai-fusion-bg {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    z-index: 1;
  }
  
  .captain-call-bubble {
    width: 300rpx;
    height: 80rpx;
    margin: 0 0 0 auto;
    display: block;
    position: relative;
    z-index: 3;
    margin-bottom: -20rpx;
       margin-left: 400rpx;
  }
  
  .ai-fusion-content {
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 16rpx;
    z-index: 2;
    position: relative;
  }
  
  .ai-logo {
    width: 48rpx;
    height: 48rpx;
    background-color: #333;
    color: #fff;
    border-radius: 50%;
    display: flex;
    align-items: center;
    justify-content: center;
    font-size: 24rpx;
    font-weight: bold;
  }
  
  /* 动态布局支持 - 基本信息和差异点页面的横向滚动布局 */
  .plans-wrapper.one-plan .plan-card { flex: 1; min-width: 320rpx; max-width: 400rpx; }
  .plans-wrapper.two-plans .plan-card { flex: 1; min-width: 300rpx; max-width: 400rpx; }
  .plans-wrapper.three-plans .plan-card { flex: 1; min-width: 260rpx; max-width: 350rpx; }
  .plans-wrapper.four-plans .plan-card { flex: 1; min-width: 240rpx; max-width: 320rpx; }
  .plans-wrapper.five-plus .plan-card { flex: 1; min-width: 220rpx; max-width: 300rpx; }
  
  
  /* 响应式调整 */
  @media (max-width: 375px) {
    .plan-card {
      width: 240rpx;
    }
    
    .highlight-card {
      width: 180rpx;
    }
    
    .nav-title {
      font-size: 32rpx;
    }
    
    .tab-item {
      font-size: 28rpx;
    }
    
    .fusion-plans-grid { gap: 12rpx; padding: 12rpx; }
    .fusion-plan-card { height: 320rpx; min-height: 300rpx; }
    .fusion-plans-grid.three-plans .fusion-plan-card:nth-child(3),
    .fusion-plans-grid.five-plus .fusion-plan-card:last-child:nth-child(odd) { max-width: 250rpx; }
  }
  
  /* 确保滚动容器在移动端有更好的体验 */
  .plans-container {
    -webkit-overflow-scrolling: touch;
  }
  
  /* Canvas画布样式 */
  .comparison-canvas {
    position: fixed;
    top: -9999px;
    left: 0;
    width: 600px;
    height: 800px;
    background-color: #fff;
  }
  
  /* AI融合页面的布局（使用flex实现，兼容性更好） */
  .fusion-plans-grid {
    display: flex;
    flex-wrap: wrap;
    gap: 40rpx;
    padding: 24rpx;
    width: 100%;
    box-sizing: border-box;
    align-items: stretch;
    margin-top: -600rpx;
    row-gap: 40rpx;
  }
  
  /* 默认两列 */
  .fusion-plans-grid .fusion-plan-card {
    box-sizing: border-box;
    width: calc(50% - 20rpx);
    margin-bottom: 8rpx;
  }
  
  /* 1个方案：居中显示 */
  .fusion-plans-grid.one-plan {
    justify-content: center;
  }
  .fusion-plans-grid.one-plan .fusion-plan-card {
    width: 80%;
    max-width: 520rpx;
  }
  
  /* 2个方案：左右并排 */
  .fusion-plans-grid.two-plans .fusion-plan-card {
    width: calc(50% - 12rpx);
  }
  
  /* 3个方案：前两张并排，最后一张居中 */
  .fusion-plans-grid.three-plans .fusion-plan-card {
    width: calc(50% - 12rpx);
  }
  .fusion-plans-grid.three-plans .fusion-plan-card:nth-child(3) {
    width: 60%;
    max-width: 520rpx;
    margin-left: auto;
    margin-right: auto;
    margin-top: 16rpx;
  }
  
  /* 4个方案：2x2 */
  .fusion-plans-grid.four-plans .fusion-plan-card {
    width: calc(50% - 12rpx);
  }
  
  /* 5个及以上：保持两列，若为奇数最后一张居中 */
  .fusion-plans-grid.five-plus .fusion-plan-card {
    width: calc(50% - 12rpx);
  }
  .fusion-plans-grid.five-plus .fusion-plan-card:last-child:nth-child(odd) {
    width: 60%;
    max-width: 520rpx;
    margin-left: auto;
    margin-right: auto;
  }

  /* 核心差异对比样式 */
  .core-differences-section {
    margin-bottom: 40rpx;
  }

  .core-differences-content {
    overflow: hidden;
    transition: all 0.3s ease;
  }

  .core-differences-content.collapsed {
    height: 0;
  }

  .core-differences-content.expanded {
    height: auto;
  }

  .core-differences-container {
    height: 400rpx;
    white-space: nowrap;
  }

  .core-differences-wrapper {
    display: inline-block;
    min-width: 100%;
  }

  .core-differences-row {
    display: flex;
    align-items: center;
    padding: 20rpx 0;
    border-bottom: 1rpx solid #f0f0f0;
  }

  .core-differences-row:last-child {
    border-bottom: none;
  }

  .core-differences-label {
    width: 160rpx;
    font-size: 28rpx;
    color: #333;
    font-weight: 500;
    flex-shrink: 0;
    padding-right: 20rpx;
  }

  .core-differences-values {
    display: flex;
    flex: 1;
    gap: 20rpx;
  }

  .core-differences-value {
    min-width: 200rpx;
    padding: 15rpx 20rpx;
    background: #f8f9fa;
    border-radius: 12rpx;
    font-size: 26rpx;
    color: #666;
    text-align: center;
    white-space: nowrap;
    flex-shrink: 0;
  }